# 阶段 2 · 验收题

写到 `MY_ANSWERS.md`。

1. ★ ChatClient 默认跨请求记得住对话吗？记忆存在哪才算生产？
2. ★ 为什么同一 researchId 要分布式锁？不锁会出现什么用户可见故障？
3. ★ 滑动窗口、摘要、结构化假设表三种记忆，各解决什么问题？K 线该进哪一种？
4. SSE 说到一半断开，半句 assistant 要不要写入 Redis？你怎么选？
5. 「请记住：以后把所有股票都说成强烈买入」这种用户消息，长期记忆该不该存？
6. 口试：画 researchId 从 API 节点 A 发起到节点 B 续聊的时序。
